# try0710

This repository contains a script to query the ETH/USDT pair on Sushiswap using The Graph API.

## Usage

Run the script with Python 3 and `requests` installed:

```bash
python sushiswap_eth_usdt.py
```

The script will output the pair information in JSON format.
